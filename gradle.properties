# The Kotlin stdlib is provided by the IntelliJ platform
kotlin.stdlib.default.dependency=false
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048m
# Build cache: reuses the outputs of unchanged tasks (locally and in CI, where
# gradle/actions/setup-gradle persists the Gradle cache between runs)
org.gradle.caching=true
