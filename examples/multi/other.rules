// Namespace deliberately NOT included by Policy: its rule is invisible from
// policy-entrypoints.rules (demonstrates namespace resolution).
Namespace Other

Context Widget {
    String name
}

Rule "Hidden elsewhere" On Widget.name {
    Set Hidden
}
