// Namespace "Base", included by Policy (see policy-contexts.rules)
Namespace Base

Context BaseEntity {
    String id
}

Rule "Base sanity check" On BaseEntity.id {
    Set Mandatory
    Error "base_id_mandatory" : "The identifier is mandatory"
}
